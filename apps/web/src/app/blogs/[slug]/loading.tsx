import PostSkeleton from "@/containers/Blogs/PostSkeleton";

export default function Loading() {
    return (
        <main className="page-scroll">
            <PostSkeleton />
        </main>
    );
}
