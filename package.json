{
  "name": "dival-portfolio",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "yarn workspace web dev",
    "build": "yarn workspace web build",
    "start": "yarn workspace web start",
    "lint": "yarn workspaces run lint",
    "generate:tokens": "yarn workspace web generate:tokens"
  },
  "dependencies": {
    "@dival-sehgal/design-tokens": "*",
    "@dival-sehgal/next-config": "*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "typescript": "^5"
  },
  "resolutions": {
    "react": "19.2.0",
    "react-dom": "19.2.0"
  }
}